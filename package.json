{
  "name": "mairu",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "参る — 近くの誰かが寺社のいまを見守る公開面PWA",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "tsc -b && vitest run",
    "photos:purge": "node scripts/purge-expired-photos.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.8",
    "maplibre-gl": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
