# WORKLOG

## 2026-07-23 — 第1回

### やったこと

- 発注者承認済みの技術スタックと仕様回答を実装条件として確定した。
- `mile` を独立したGitリポジトリとして初期化した。
- ライセンス、作業記録、設計判断記録の土台を作成した。
- Vite＋Vanilla TypeScriptで地図、詳細、回答、完了の4画面を実装した。
- MapLibreとOpenFreeMapで、同心円と期限付き反応による探知UIを実装した。
- Supabaseのスキーマ、RLS、非公開Storage、回答確定RPC、seedを作成した。
- 端末内で長辺1600px・約1MBへ写真を縮小し、Canvas再描画でEXIFを除去する処理を実装した。
- 90日経過写真の削除スクリプトと週次GitHub Actionsを実装した。
- 現在地が回答型、送信ペイロード、DBスキーマに入らないことを含む15テストを作成した。
- スマホ幅の実ブラウザで詳細→回答→完了を通し、確認時刻、累計マイル、重複回答拒否を確認した。
- Vite開発時のMapLibre worker最適化エラーを検出し、依存事前最適化から除外して再検証した。
- READMEにデータ方針、運営登録、座標取得、有効期限、CSV、週1回アクセス、デプロイを記載した。

### 選んだ技術判断とその理由

- UIはVite＋Vanilla TypeScriptとする。4画面の公開面にReactやルーターを持ち込まず、依存と保守範囲を小さくするため。
- Supabase依存はdata-access層に隔離する。BaaSの乗り換え時に画面ロジックを変更しないため。
- 現在地をリポジトリ引数・回答型・DB列に含めない。位置情報をクライアントの地図表示だけに閉じ込め、コードとテストで検証可能にするため。
- 匿名利用者はSupabase Anonymous Sign-Inで識別する。回答の一意制約とStorageの所有者制御をRLSで実施するため。
- `limited` ミッションはv0.1の公開APIから取得不能にする。専用URLやトークンは実装しない。
- マイルは `missions.reward_miles` に保持し、初期seedでは全件同値とする。
- 回答は `submit_answer` RPCだけを通す。サーバー時刻、匿名ID、選択肢、
  公開条件、期限、一意性、マイル集計をDB境界で確定するため。
- 写真は非公開bucketへ保存し、90日後に本体を削除して回答行のpathを
  `null` にする。集計に必要な選択回答と確認時刻は残すため。

### 未解決の課題

- Supabaseプロジェクトの作成と公開環境変数設定には、発注者アカウントへの接続が必要。
- Cloudflare Pagesへの本番公開には、発注者アカウントへの接続が必要。
- GitHub CLIのブラウザ認証は発注者の許可待ち。
- READMEには登録位置の簡略図を収録済み。実Supabaseプロジェクト接続後に
  Dashboardの表記差分を最終確認する。

### このセッションのコミット

- `65aaa8f` — 承認済みの仕様境界と設計判断を実装前に記録
- `d7c8761` — データ境界、Supabaseスキーマ、PWA基盤、テストを実装
- `0f2cde3` — 4画面の静かな探知体験を実装

## 2026-07-23 — 第2回

### やったこと

- GitHubアカウント接続後、公開リポジトリ `jtcpride/mile` を作成した。
- 写真削除ワークフローを含めて `main` をGitHubへpushした。
- GitHub Pages用のテスト、ビルド、公開ワークフローを追加した。
- `/mile/` 配下で画面遷移、PWA manifest、Service Workerが動くようにした。
- 詳細URLへ直接アクセスした場合もアプリへ戻せる404フォールバックを追加した。
- GitHub PagesをActions配信方式で有効化し、公開URLのHTTP 200とmanifestを確認した。
- 写真削除ワークフローのSecrets判定をGitHub対応の環境変数経由へ修正した。

### 選んだ技術判断とその理由

- 初回デモはGitHub Pagesへ置く。公開ソース、検証結果、配信履歴を
  一つの公開リポジトリで追跡でき、月額費用が発生しないため。
- GitHub Actions上だけ基底パスを `/mile/` にする。ローカル開発の
  既存URLを変えず、GitHubのプロジェクトサイト配下でも資産を解決するため。
- Supabase未接続の公開デモは、明示された静的デモモードで配信する。
  認証情報を仮置きせず、4画面の操作検収を先に可能にするため。

### 未解決の課題

- 実運用には発注者のSupabaseプロジェクト作成、migration適用、匿名認証有効化、
  公開用環境変数の設定が必要。
- Supabase未接続のため、公開デモではサーバー時刻・匿名認証・非公開写真保存の
  実BaaS疎通は未検証（静的デモ実装での画面検収は可能）。

### このセッションのコミット

- `c37cff9` — 公開リポジトリから検証済みPWAをGitHub Pagesへ配信
- 写真削除ワークフロー修正をこのセッションの末尾でコミットする。

## 2026-07-24 — 第3回

### やったこと

- 発注者の実機確認で、波紋、ズーム、空振り表示は正常だが背景地図が
  描画されないことを確認した。
- 公開スタイル、TileJSON、京都付近のVector Tile、glyph、spriteが
  HTTP 200で配信されていることを切り分けた。
- 地図非表示は仕様ではなく実機描画バグと判断し、MapLibreを維持したまま
  OpenStreetMap Standardのラスタータイルへ切り替えた。
- 背景地図の彩度とコントラストをMapLibre paintで抑え、寺社文脈の
  静かなトーンを維持した。
- Cloudflare PagesからGitHub Pagesへ公開先を変更した判断を
  `docs/decisions/0006-hosting-github-pages-over-cloudflare.md` に明記した。
- Supabaseアカウント・プロジェクト作成の発注者向け手順と、
  作成後のCodex作業分担を `docs/supabase-setup.md` に追加した。
- GitHub PagesのビルドがActions variablesからSupabase公開接続値を
  受け取れるようにした。
- 地図設定テストを追加し、全16テストと本番ビルドを通した。

### 選んだ技術判断とその理由

- v0.1の背景地図はラスタータイルとする。外部style、Vector Tile、glyph、
  spriteの多段取得を減らし、モバイル実機の描画確実性を優先するため。
- 新しい地図ライブラリは追加しない。既存のMapLibre、探知演出、
  マーカー実装を維持して修正範囲を限定するため。
- OSM Standardは通常の対話表示だけに使い、プリフェッチ・オフライン保存は
  行わない。ブラウザキャッシュと帰属表示を含めTile Usage Policyに従うため。
- SupabaseのPublishable keyはGitHub Actions variablesで渡す。
  公開クライアント用の値とSecret keyの運用境界を明確にするため。

### 未解決の課題

- 修正版をGitHub Pagesへ配信した後、発注者実機で背景地図を再確認する。
- Supabaseプロジェクト作成は発注者のアカウント作業待ち。
- 実Supabase接続後、匿名回答、サーバー時刻、重複拒否、非公開写真を検証する。

### このセッションのコミット

- `b1eb0ed` — モバイル実機で背景地図を確実に描画する修正
- `bf9decb` — ホスティング判断を記録しSupabase引き継ぎを準備

## 2026-07-25 — 第4回

### やったこと

- 発注者作成済みのSupabase Freeプロジェクトへ初期migrationと架空寺社seedを適用した。
- Anonymous Sign-Inを有効化し、非公開 `answer-photos` bucketと3本のStorage policyを確認した。
- GitHub Actions variablesへProject URLとPublishable keyを登録し、Pagesを再配信した。
- 実Data APIの初回検証で `missions` のPostgres読取権限不足を検出した。
- `missions` だけに `anon` / `authenticated` のSELECT権限を与える追加migrationを適用した。
- 公開版で実ミッション取得、匿名回答、サーバー確認時刻、3 mile付与、
  DB一意制約による同一端末の重複回答拒否を確認した。
- Database password、Secret key、`service_role` keyは取得・共有・登録しなかった。
- 全17テストと本番ビルドを通した。

### 選んだ技術判断とその理由

- RLSとは別に `missions` のSELECT権限だけを付与する。Data APIがRLSを
  評価できるようにしつつ、`limited`、期限切れ、下書きは既存RLSで隠すため。
- `answers` には直接権限を付与しない。回答・時刻・匿名ID・マイルを
  引き続き `submit_answer` RPCだけで確定するため。
- Supabaseの新しいPublishable keyだけをPagesへ渡す。ブラウザに不要な
  権限キーを接続作業へ持ち込まないため。

### 未解決の課題

- 任意写真の実ファイルアップロードは未実施。bucketの非公開設定、容量上限、
  MIME制限、所有者policyはDashboardとmigrationで確認済み。
- 90日写真削除ワークフローを本番実行するには、ブラウザへ渡さないSecret keyを
  GitHub Actions Secretsへ別途安全に設定する必要がある。
- Anonymous Sign-Inの濫用対策として、初期利用状況を見てTurnstile導入を検討する。

### このセッションのコミット

- `72fead3` — Data APIへ公開ミッションだけを読める最小権限を追加
- Supabase接続状況と検証結果の文書更新を、このセッション末尾でコミットする。

## 2026-07-25 — 第5回

### やったこと

- 実機フィードバックを受け、「反応なし」の結果カードを常設表示から
  約5秒後に自動消去する一時通知へ変更した。
- 消去直前に300msのフェードを入れ、唐突に消えないようにした。
- 結果消去後も地図、現在地精度、プライバシー文言、「もう一度探知」ボタンを残した。
- 地図内の補助文言を「場所を変えたら『もう一度探知』で再確認できます」へ
  切り替え、再操作の導線を維持した。
- 連続探知時に古い自動消去タイマーが新しい結果を隠さないよう、取消処理を追加した。
- 自動消去とタイマー取消の2テストを追加し、全19テストと本番ビルドを通した。
- GitHub Pagesへ配信し、実ブラウザで空振り結果の表示後、約5秒で非表示になることを確認した。
- 写真アップロード確認用に、別の新しい匿名ブラウザセッションで
  「青葉神社・石灯籠」の回答画面を直接開いた。
- 任意写真1枚、単一選択肢、送信ボタンが見える状態で止め、
  写真選択・回答送信は発注者に委ねた。
- 既回答のChromeセッションで写真送信が通信エラー表示になる事象を調査した。
- 原因はDBの重複回答エラーが通常の `Error` ではなくplain objectで返り、
  既存の正規化処理が詳細を `[object Object]` として捨てていたことだった。
- Supabaseエラーの `code`、`message`、`details` 等を安全に読み、
  PostgreSQL `23505` を「この端末からは、すでに回答済み」と正確に表示するよう修正した。
- 実ブラウザの既回答セッションで、汎用通信エラーが出ず重複回答文言になることを確認した。
- 修正版を反映した別ブラウザの新規匿名セッションに写真回答画面を開き直し、
  発注者が安全な画像を選んで実送信できる状態で止めた。
- エラー正規化テストを追加し、全21テストと本番ビルドを通した。

### 選んだ技術判断とその理由

- 新しい依存は追加せず、ブラウザ標準タイマーとCSS transitionだけを使う。
  5秒の一時表示に状態管理ライブラリを増やす必要がないため。
- 空振りカードだけを自動消去し、ミッション反応カードは期限情報と回答導線として残す。
- 写真確認は既回答のChromeセッションと分離した新しい匿名ブラウザで行う。
  DBの1端末1ミッション1回答制約を弱めたり、既存回答を削除したりしないため。
- 重複判定は表示文言の文字列だけでなくPostgreSQLエラーコード `23505` も見る。
  Supabase SDKがplain objectを返しても、通信障害と誤認しないため。

### 未解決の課題

- 写真アップロードの最終確認は、発注者が安全なテスト画像を選択して送信した後に行う。
- 現在のseedミッションには有効期限があるため、期限後の再試験時はseedを再投入する。

### このセッションのコミット

- `97563fa` — 空振り結果を約5秒後に自然に消す
- `48b6cba` — 自動消去と写真確認用匿名画面の検証記録
- `495fdc0` — 重複回答を通信エラーと誤表示しない修正
- 追加調査結果の記録を、セッション末尾でコミットする。
